# set_draw_target([ud])

## Overview

Set the current draw target to userdata u8 `ud`; all draw operations go to this 

When `ud` is not given; it defaults to the current display.

## Arguments

### `[ud]`: userdata

A 2d userdata u8