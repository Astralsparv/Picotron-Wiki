# cls([color])

## Overview

`cls` clears the draw target by filling it with a color

Often used in `_draw()` at the start of the frame to flush the previous frame.

Source: [source.lua](source.lua)

## Arguments

### [color]

Defaults to 0 (black)

The color to draw to the draw target

## Returns

Unknown