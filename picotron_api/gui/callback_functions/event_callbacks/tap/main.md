# tap

## Overview

Triggered if a mouse button is released within 0.3 seconds of being pressed and the mouse hasn’t moved.

Use `msg.last_mb` if you want to check that a specific mouse button was released.

The time duration is hard coded

## msg values:

### last_mb
    `msg.mb` from previous frame