# gui_el:detach()

## Overview

Remove self (gui_el) from the parent's list of child elements.

## Returns

### `detached`: table

The detached element.

# gui_el:detach(el)

## Overview

Detach element `el` from self's list of child elements.

## Returns

### `detached`: table

The detached element.